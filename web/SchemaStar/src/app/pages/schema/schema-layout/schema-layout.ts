import { Component } from '@angular/core';
import { Schema } from '../schema-components/schema/schema';

@Component({
  selector: 'app-schema-layout',
  imports: [Schema],
  templateUrl: './schema-layout.html',
  styleUrl: './schema-layout.css',
})
export class SchemaLayout {

}
